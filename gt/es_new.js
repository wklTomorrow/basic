'' || 'hello world' // ''
'' ?? 'hello world' // hello world ?? 前半部分必须是 null 或者undefine

/**
 * let zz = data && data.a && data.a.b === let s = data?.a?.b
 * s ===zz
 * @param{}
 * @returns {number}
 */